version: 2.1
jobs:
  build:
    docker:
      - image: 'dmoody256/scons-test-image:scons-test'
    steps:
      - run: python --version
      - checkout
      - run: python runtest.py -a -t -j 2 || if [[ $? == 2 ]]; then true; else false; fi